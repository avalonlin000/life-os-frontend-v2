# 2026-07-19｜R1 Agent 陪伴与轻量整理模块

## Goal

把结衣 Agent 的日常对话第一块拆成两个清楚的模块：陪伴，以及轻量整理。

## 本次完成

- `结衣` profile Skill 增加日常对话路由：情绪先走陪伴，混乱/逃避/简短复盘走轻量整理。
- 新增 `companion.md`：定义被接住、只陪伴、不强塞方案、停止条件和安全边界。
- 新增 `lightweight-organizing.md`：定义一条主线、一个下一步、短输出、无产品/任务/记忆写入边界。
- SOUL、兼容 reference、统筹指令移除这两类行为的重复旧模板，改为引用模块合同。
- BOT_GUIDE、AGENT-CAPABILITY-MAP、BACKLOG、STATUS 写回模块责任和当前状态。

## 验证

- `tests/agent_behavior/test_jieyi_conversation_modules.py`：5 项通过。
- fresh CLI 新会话实测：只想陪伴、混乱收束、逃避拉回、情绪+整理、说“够了”五种场景均有回复；“够了”场景不再调用工具。
- 实测会话只调用 `skill_view` 读取对应模块，没有调用 memory/todo/产品写入工具；profile memory 文件时间未变化。
- `hermes-gateway-jieyi.service` 已重启并恢复 Feishu WebSocket connected。
- 真实 Feishu 用户消息回放尚未执行，因此本切片状态为 Implemented，不标 Verified。

## 给结衣

结衣现在会先判断你是想被陪伴，还是想把混乱收成一条主线。只说“先陪我”时不会硬塞方案；说“帮我整理”时只给一个下一步；默认不会替你写任务、笔记、原则或长期记忆。
