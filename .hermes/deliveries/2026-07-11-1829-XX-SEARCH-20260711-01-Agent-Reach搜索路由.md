# XX-SEARCH-20260711-01 小雪 Agent Reach 搜索路由

生成时间：2026-07-11 18:29 BJT
Change ID：`XX-SEARCH-20260711-01`

## 任务

把已安装的 Agent Reach 正式接入小雪的搜索路由，并与内部事实、日报正式舆情、BP/盘口判断链明确分流。

## 变更文件

- `/home/ubuntu/.agents/skills/xiaoxue-esports-toolkit/references/search-routing.md`：新增可执行搜索路由合同和验收示例。
- `/home/ubuntu/.agents/skills/xiaoxue-esports-toolkit/SKILL.md`：小雪搜索意图强制先进入路由合同。
- `/home/ubuntu/.agents/skills/xiaoxue-esports-workflow/SKILL.md` 与 `references/full-legacy-skill.md`：加入 Agent Reach 调用和禁入边界。
- `docs/products/xiaoxue-esports-life/SSD/05-search-routing.md`：记录四层搜索架构与验收规格。
- `BOT_GUIDE.md`、`PROJECT_INDEX.md`、`BACKLOG.md`、`ACCEPTANCE.md`：同步机器人说明、索引、状态和验收。
- `/home/ubuntu/xiaoxue-web/memory-bank/modules.md`、`progress.md`：同步 runtime 项目事实。
- `/home/ubuntu/.hermes/team/sync/{project,codex,xiaoxue}-latest.md`：写入 scoped sync。

## 关键决策

- 小雪内部 DB、ScoreGG、Wiki、TK/RAG 是事实首选；不为了联网重复搜。
- Agent Reach 负责最新外部资料、官方网页、外部链接和视频发现。
- 日报 `public_opinion` 仍只走 `lol-daily-online-sources` / 豆包冻结链路。
- BP、阵容和盘口仍走 `lol-lineup-analysis` 与结构化数据。
- 本轮不改日报代码、DailyContext schema、数据库、cron、前端或外部平台账号。

## 验证结果

- `skillctl doctor`：220 个 skill，220 个唯一名称，0 重复。
- `agent-reach doctor --json`：Exa、Jina、YouTube、RSS、B站基础搜索可用；登录型社交渠道按实时状态降级。
- 真实 Exa 烟测：成功返回 2026-07 LOL 26.14 版本资料。
- 三个 Hermes gateway：default、jieyi、xiaobai 均 active，并已连接飞书。
- 四个 skill 入口均解析到 `/home/ubuntu/.agents/skills`。
- 路由合同静态验收：6 个关键文件、全部边界字段通过。
- 两个工作树的目标文件 `git diff --check` 通过。
- 执行 `skillctl reload` 后三个 gateway 重新加载并恢复 active。

## 风险与遗留

- Twitter、Reddit、小红书等登录型社交渠道尚未配置；当前不会把它们假装成可用，也不会静默回退豆包。
- life-os 和 xiaoxue-web 在本任务前已有大量未提交改动；本次未创建 Git commit，避免把既有改动混入本任务。
- Agent Reach 当前只服务交互式普通查询和知识发现，未自动写入日报冻结材料包。

## 给小雪

遇到“查一下、搜一下、找最新、打开链接、搜 B站/YouTube”时先按搜索路由分流：内部事实优先，普通外部资料走 Agent Reach，日报正式舆情走冻结豆包链路，BP/盘口走阵容八步法。

## 给结衣

无影响。

## 后续动作

无必须动作。后续如需把 Agent Reach 自动写入日报，必须另开开发切片定义冻结包 schema、来源校验和失败降级。
