--- before//home/ubuntu/.agents/skills/lol-lineup-analysis/tests/test_skill_contract.py
+++ after//home/ubuntu/.agents/skills/lol-lineup-analysis/tests/test_skill_contract.py
@@ -33,17 +33,19 @@
         for token in ("strength_score.py", "唯一归因表", "十英雄兑现表", "六项阵容检查"):
             self.assertNotIn(token, text)
 
-    def test_match_profile_uses_calibrated_conversion_rules(self) -> None:
+    def test_match_profile_gives_a_fixed_30_minute_direction(self) -> None:
         text = (ROOT / "SKILL.md").read_text(encoding="utf-8")
         for token in (
             "后期英雄",
             "人头先看双方能不能反复接战",
             "走势先看优势能不能连续兑现",
             "双方都能主动把局面打回来",
-            "不要再用“关键团在20–30分钟发生”代替结束时间",
-            "20–30分钟形成胜势但32分钟推掉，仍写`30–35分钟`",
-            "结束只回答基地大概何时被推掉",
-            "30–35分钟",
+            "固定以30:00为唯一分界",
+            "偏小 / 偏大 / 放弃",
+            "偏小：更倾向在30:00之前推掉基地",
+            "偏大：更倾向在30:00之后才推掉基地",
+            "信号互相冲突",
+            "直接给`放弃`",
             "单向推进`可以只是55/45的高滚雪球形态",
             "两条以上彼此独立的抓人或强开路线",
             "不能只写成“前期略优、后期自动被翻”",
@@ -52,6 +54,13 @@
             "直接判断“高”",
         ):
             self.assertIn(token, text)
+
+        for forbidden in (
+            "30分钟前 / 30–35分钟 / 35分钟后",
+            "30–35分钟",
+            "结束时点",
+        ):
+            self.assertNotIn(forbidden, text)
 
         self.assertNotIn("双方都有开团，所以更偏有来有回", text)
 
