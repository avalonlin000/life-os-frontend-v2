# XX-FIXED-20260712-01 小雪固定工作流收口

生成时间：2026-07-12 16:26 BJT
Change ID：`XX-FIXED-20260712-01`
代码稳定点：`lol_data@57b4ffa`

## 任务

把小雪项目中高确定性的环节落地为可测试、可审计、失败关闭的 Fixed / Hardcoded Workflows，并保留 BP 判断与知识入库的人工确认边界。

## 变更文件

- `/home/ubuntu/lol_data/scripts/data_readiness_pipeline.py`：固定 ScoreGG → TS → 数据校验顺序，原子写 readiness manifest；缺库、坏库、超时均失败留痕。
- `/home/ubuntu/lol_data/scripts/daily_pipeline.py`：publish 前强制校验当天 full readiness；check-only、畸形阶段或失败状态不放行。
- `/home/ubuntu/lol_data/scripts/xiaoxue_knowledge_import.py`：知识导入先生成候选 manifest；登录暂停、发现失败、截断 JSON、非法编码和坏结构显式分支。
- `/home/ubuntu/xiaoxue-web/config/workflows.json`、`workflow_contracts.py`：配置化今日内容、队伍别名、盘口枚举、BP 八步契约和复盘模板。
- `/home/ubuntu/xiaoxue-web/main.py`、`src/main.js`、`src/workflow-ui.js`：新增 BP prepare、复盘 preview/confirmed commit、健康门禁与网络错误恢复。
- `/home/ubuntu/.hermes/scripts/xiaoxue_data_readiness.py`、`xiaoxue_daily_inspection.py`：Cron 固定入口与巡检自锁修复。
- `/home/ubuntu/lol_data/tests/test_fixed_workflows.py`、`/home/ubuntu/xiaoxue-web/tests/`：覆盖正常路径和失败关闭边界。
- 小雪 runtime memory-bank、产品 STATUS/BACKLOG/CRON/ACCEPTANCE/SSD、日报与 Skill 文档：同步当前生产事实，移除旧日报链路漂移。

## 关键决策

- 数据放行必须来自当天完整刷新：`mode=full / ok=true`，且 ScoreGG、TS 两阶段均 `status=ok / exit_code=0`；check-only 只诊断。
- BP 固定输入校验、八步顺序、决策枚举和安全边界；具体阵容强弱仍由小雪动态判断。
- 赛后复盘必须 preview 后显式确认，只写回原 `market_notes`；不自动写 TK、画像或发出交易指令。
- 知识导入只允许消费 manifest 中候选；微信读书登录暂停不掩盖 B站发现/清单损坏。
- 日报仍只有 `daily_pipeline.py --publish` 一个生产入口；本轮只做 dry-run，没有发布或覆盖飞书/Wiki。

## 验证结果

- `lol_data`：43 项全量测试通过；独立审查合计 60 项相关测试通过，0 安全问题、0 逻辑错误。
- `xiaoxue-web`：15 项 Python 测试、2 项 Node 测试、Vite production build 全部通过。
- 工作台 acceptance：health、market_notes CRUD、画像、版本理解全部通过。
- 真实数据刷新：289 条 schedules、263 条 matches 更新；31/31 队伍 TS 覆盖；health `data_readiness.status=ready`。
- 日报 `2026-07-12` dry-run：`audit_ok=true`、`validation_errors=[]`、七日 TK 91 条、`published=false`。
- BP runtime：完整双方五位置输入返回 `ready=true`、固定八步和 `保留/降权/取消/等待`。
- Cron：数据就绪 05:30 启用；旧独立 TS 暂停；每日巡检真实触发成功并刷新 `last_status=ok`。
- `git diff --check`、Python compile、API service、Vite service 均通过。

## 风险与遗留

- 根分区使用率 86.9%，处于 85%-92% 警告区，但未达到强制清理阈值。
- 微信读书当前 `NEED_LOGIN`，知识导入会显式暂停，不会回退旧静态二维码。
- `xiaoxue-web` 与 `life-os-frontend-v2` 在任务开始前已有大量重叠未提交改动；本轮未创建这两个仓库的 commit，避免混入既有工作。原本干净的 `lol_data` 已提交为 `57b4ffa`。

## 可见范围

| 对象 | 可见内容 |
|------|----------|
| Codex | 全量代码、测试、Cron、文档、运行验证与风险 |
| 小白 | Cron 固定入口、服务状态、磁盘警告、Git 稳定点和接手边界 |
| 结衣 | 只需知道小雪侧固定工作流已更新 |
| 小雪 | 数据门禁、BP 八步入口、复盘确认流、知识导入暂停语义和日报唯一入口 |

## 给小雪

现在高确定性环节已经固定：每日先刷新 ScoreGG 和 TS 并过门禁；阵容齐全后按八步法整理；赛后复盘先预览再确认，只回写原盘口草稿；知识导入遇登录或坏清单会明确暂停，不会假装成功。

## 给结衣

小雪侧完成了一轮固定工作流和失败保护升级；结衣侧无功能变更。

## 后续动作

- 方便时恢复微信读书活码登录。
- 根分区超过 92% 前安排一次安全清理；当前仅警告。
