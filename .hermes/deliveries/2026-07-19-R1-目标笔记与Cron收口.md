# 结衣 R1：目标、笔记与 Cron 收口

日期：2026-07-19

## 已完成

- 新增 `GoalService`、`NoteService`，目标/笔记 API 不再依赖 `JieyiAgent`。
- `/api/daily-note` 只读 `refactor_data.db.notes`；没有结衣笔记就明确返回空态，不跨到小雪 wiki。
- 新增 `DailyPlanAIService` 和 `/api/daily-plan/generate`：只使用结衣产品知识生成，验证结构后才写 `daily_plan`，并同步行动。
- 每日整理在模型错误或无真实摘要时不写入。
- 04:00 旧共享文件任务暂停；04:05 计划任务改为结衣 profile 的 no-agent 产品脚本；23:00 每日整理从 xiaobai profile 移交到 jieyi profile。
- 所有脚本都在写入后 GET 读回正式数据源；旧任务和配置均保留时间戳备份。

## 验证

- `PYTHONPATH=backend pytest -q` → 31 passed。
- 后端服务重启后 PID 替换为 `2662786`，`/api/goals`、`/api/notes`、`/api/daily-note` 返回稳定产品数据。
- `jieyi` / `xiaobai` 网关重启后均 active，Feishu WebSocket 已连接。
- Cron scheduler heartbeat 正常；jieyi profile 当前 2 个 active no-agent 任务：04:05 计划、23:00 整理；两个 `--check` 脚本通过。
- 本轮没有手动执行真实生成，避免提前写入当天业务数据；定时任务会按新责任链运行。

## 结果

结衣的目标、笔记、每日计划和每日整理现在都回到结衣产品正源；Agent 只负责交互和触发稳定产品入口，不再直接读写小雪目录或 legacy 课程表文件。

## 2026-07-19 真实 Cron 重跑验收

- 04:05 新任务在当天首次自动运行时成功写入并读回，但暴露出最近 8 条知识均为 `xiaobai-smoke-test` 联调材料，导致生成内容来源不合格。
- `DailyPlanAIService` 已过滤标题、正文、标签或来源中带内部 smoke 标记的材料，并把候选读取窗口扩大后再选取最多 8 条正式材料；新增回归测试固定该边界。
- 修复后通过 Hermes Cron 调度器手动触发同一任务，运行时间 `2026-07-19 10:57:55 +08:00`，状态 `ok`、无错误。
- 正式读回：`source=product_db`、`status=available`，学习 2 条、复习 1 条、行动 3 条；内容中不含 smoke 标记。
- 行动同步读回 3 条，今日聚合返回 `act_actions=false`（非空态）、`action_total=3`；3001 产品代理入口读到相同结果。
- 重跑前 3 条旧行动均为未完成，因此本次替换没有丢失用户打卡进度；数据库已保留运行前备份。
- 后端全量回归为 32 passed；后端、结衣网关与 Cron ticker 均保持 active。

### 23:00 每日整理真实重跑

- 运行前事实：当天没有 mood、activities 或已完成行动，`daily_review` 为空。
- 第一次实跑暴露出模型会把“今天没有记录，无法总结”返回为非空 summary，旧逻辑因此把它误存并在思页标成“已生成”。
- `DailyReviewAIService` 已把无数据判断前移：没有 mood、activity 或 trade 时不调用模型，直接返回 `status=skipped, reason=no_daily_records`，且不写数据库；回归测试覆盖“不得调用模型、不得写入”。
- 本次验收产生的误存记录已按日期和精确 summary 校验后清理，运行前数据库备份保留；其他计划和行动未变更。
- 修复后通过 job `7078a2e9fc95` 再次正式触发，运行时间 `2026-07-19 11:05:34 +08:00`，Cron 状态 `ok`、无错误，输出明确为“当天没有足够的真实记录，已跳过”。
- 后端与 3001 代理均读回 `summary=null`；数据库该日期行数为 0；今日聚合保持 `reflect_review=true` 空态和“今日整理还没有生成”。
- 下一次自动运行仍为 `2026-07-19 23:00 +08:00`，届时只会基于当天新增的真实心情、活动或交易记录生成。

### 有真实心情后的重跑

- 用户保存了当天心情记录：心情 5/10，并写下“今天是恢复期，我想要从不断的亏损中走出来，不求大胜但是自己心态不能乱”。
- 首次重跑确认记录已进入产品表，但发现每日整理适配层只传心情分数，没有传用户填写的正文；模型因此仍判断信息不足。
- 已补上心情正文上下文，并新增回归测试；后端回归更新为 33 passed。
- 11:11 通过同一 23:00 Cron 再次实跑成功，生成内容明确提到恢复期、当前没有活动/交易记录，并给出明天记录一件踏实小事的建议。
- 后端与 3001 代理读回一致；思页聚合 `review_status=available`，同时保留原始心情正文；Cron 状态 `ok`，下一次仍为今晚 23:00。
