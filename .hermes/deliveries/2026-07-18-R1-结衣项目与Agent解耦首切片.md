# 结衣项目与 Agent 解耦：R1 首切片交付记录

日期：2026-07-18

## 本次完成

1. 完成 PRD、SSD、BOT_GUIDE、STATUS、BACKLOG 与真实运行链路审计，补齐项目能力地图与 Agent 能力地图。
2. 新增 `DailyPlanService`：产品数据库优先；旧日课 JSON 只作为标记为 `source=legacy_file` 的兼容读取；显式 refresh 才导入产品数据库并同步行动。
3. `/api/daily-plan` 与 `/api/agent/jieyi/daily-context` 统一使用同一套 DailyPlan 读取契约；旧 Agent 刷新入口不再改写小雪目录。
4. 结衣普通 Feishu 会话收窄为陪伴、记忆、会话检索、澄清、知识查询等能力；移除终端、文件、浏览器、视觉、cron、委派等工程越界工具。
5. 在 SOUL 与结衣 Skill 增加工程请求硬门：项目请求只整理产品目标/现象并交给小白，不索要技术路径，不直接改项目。

## 验证证据

- 后端：`PYTHONPATH=backend pytest -q` → 8 passed。
- Python：DailyPlan service、Agent、路由 `py_compile` 通过。
- 前端：`pnpm build:jieyi` → TypeScript 与 Vite 构建通过。
- 服务/API：`jieyi-backend.service` 与 `hermes-gateway-jieyi.service` active；health、daily-plan、daily-context、aggregate 均 HTTP 200。
- 当前日期 2026-07-18 的 DailyPlan 返回 `source=legacy_file`、`status=compatibility`，内容可读；daily-context 同样返回该计划来源。
- Hermes 运行时解析结果不含 `terminal`、`file`、`browser`、`cronjob`、`delegate_task`；fresh CLI 会话验证工程请求只停在交接层。

## 明确未声称完成

- 尚未执行真实的 refresh 写入，避免未经确认改动当前生产计划与行动。
- `JieyiAgent` 仍承载知/行/思多块旧逻辑；下一阶段继续拆产品 Service/Repository 和 Agent 适配层。
- 认知资产正式 Wiki 写回、深度学习质量、模式/阻力/趋势后端契约仍按审计结论保留为后续阶段。
